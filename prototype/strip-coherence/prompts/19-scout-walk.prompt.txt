TRUE chunky pixel art sprite strip ONLY. Four WALK CYCLE frames of a small fantasy SCOUT in one horizontal row, facing RIGHT.

**Canvas:** very wide panoramic strip — aspect ratio roughly 3:1 (much wider than tall). The four frames must fill most of the width.

Each frame on an exact **16×24 logical pixel grid rendered large** — every logical pixel is one clean flat square block. No anti-aliasing, blur, gradient, or dithering.

Scout: green hooded cloak, short bow on the back, dagger at the belt. **Standard four-step walk cycle: contact, down, pass, up.** Legs clearly change position each frame. The scout **stays centered horizontally — do not translate the character across the strip**.

Between each frame: **two full magenta logical cells** of empty gutter (no character pixels in gutter columns).

Flat solid **magenta `#ff00ff`** background everywhere else. Selective 1px dark outline. Upper-left light. **8–12 flat colors max** — same palette in every frame.

Frame order left→right: walk_0, walk_1, walk_2, walk_3 — **same character, loopable**.

Do not add margins, labels, numbers, or extra rows. One row of four frames only.
