TRUE chunky pixel art sprite strip ONLY. Four idle frames of a small fantasy MINER in one horizontal row, facing RIGHT.

**Canvas:** very wide panoramic strip — aspect ratio roughly 3:1 (much wider than tall). The four frames must fill most of the width.

Each frame on an exact **16×24 logical pixel grid rendered large** — every logical pixel is one clean flat square block. No anti-aliasing, blur, gradient, or dithering. **Large crisp square pixels only — match the visual style of a retro game sprite sheet.**

Miner: feet planted on the bottom row. **Same miner, same green tunic and blue helmet** in all frames.

**IMPORTANT — cohort spread without identity swap.** Four micro-variations of the SAME miner idle: frame 0 neutral, frame 1 head tilted slightly right, frame 2 head tilted slightly left, frame 3 slight body lean. Neighbors change only a little; **no two frames are pixel-identical** but all are clearly the same character. Shared earthy palette, no recolouring.

Between each frame: **two full magenta logical cells** of empty gutter (no character pixels in gutter columns).

Flat solid **magenta `#ff00ff`** background everywhere else. Selective 1px dark outline.

Frame order left→right: idle_0, idle_1, idle_2, idle_3.

Do not add margins, labels, numbers, or extra rows. One row of four frames only.

<!-- Gate control retry: idle / min_pair_cohort_pass. Clear grid blocks; same subject cohort. -->
