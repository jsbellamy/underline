TRUE chunky pixel art sprite strip ONLY. Four idle animation frames of a small SLIME BLOB in one horizontal row.

**Canvas:** very wide panoramic strip — aspect ratio roughly 3:1 (much wider than tall). The four frames must fill most of the width.

Each frame on an exact **16×24 logical pixel grid rendered large** — every logical pixel is one clean flat square block. No anti-aliasing, blur, gradient, or dithering.

Slime: translucent green dome with two dark eye pixels and one lighter highlight block. It sits **flat on the bottom row of each frame**. **Same green slime, same palette in every frame** — identical colors across all four frames.

**IMPORTANT — silhouette spike on ONE transition only.** Frame 0: normal squat dome idle. Frame 1: nearly identical to frame 0 (tiny breathing only). Frame 2: **dramatic stretch** — dome becomes much taller and narrower, eyes move up, large occupancy change from frame 1. Frame 3: return to the same squat dome shape as frame 0 (loop closure).

Between each frame: **two full magenta logical cells** of empty gutter (no character pixels in gutter columns).

Flat solid **magenta `#ff00ff`** background everywhere else. Selective 1px dark outline. Upper-left light. **6–10 flat colors max**.

Frame order left→right: idle_0, idle_1, idle_2, idle_3.

Do not add margins, labels, numbers, or extra rows. One row of four frames only.

<!-- Gate control: blob_idle / silhouette_budget. Same slime palette; one adjacent pair
     has a large occupancy change; frame 3 matches frame 0 for loop closure. -->
