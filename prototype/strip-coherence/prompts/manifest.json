{
  "_comment": "Expected verdict per corpus sample. Drop <id>.png into inbox/ and run `npm run prototype:strip:corpus`. 'expect' is the prediction made BEFORE generating — do not edit it to match results; a wrong prediction is the finding.",
  "samples": [
    {
      "id": "01-miner-idle",
      "class": "idle",
      "grounded": true,
      "expect": "PASS",
      "expect_gates": [],
      "why": "baseline sample the 0.28/0.15 budgets were derived from"
    },
    {
      "id": "02-slime-idle",
      "class": "idle",
      "grounded": true,
      "expect": "PASS",
      "expect_gates": [],
      "why": "curved limbless silhouette — gate must not be keyed to the miner's shape"
    },
    {
      "id": "03-torch-flicker",
      "class": "emissive",
      "grounded": true,
      "expect": "PASS",
      "expect_gates": [],
      "why": "legitimate color churn; honest stress test of the 0.15 drift budget"
    },
    {
      "id": "04-bat-flap",
      "class": "airborne",
      "grounded": false,
      "expect": "PASS",
      "expect_gates": [],
      "why": "no planted baseline — tests an assumption the idle strip could not"
    },
    {
      "id": "05-miner-walk",
      "class": "locomotion",
      "grounded": true,
      "expect": "FAIL",
      "expect_gates": ["silhouette_budget"],
      "why": "a correct walk legitimately exceeds an idle-derived budget; failure here means budgets are per-motion-class"
    },
    {
      "id": "06-miner-swing",
      "class": "one-shot",
      "grounded": true,
      "expect": "FAIL",
      "expect_gates": ["loop_closure_pass"],
      "why": "non-looping action; adjacent motion should stay sane while loop closure blows out"
    },
    {
      "id": "07-NEG-palette-drift",
      "class": "negative",
      "grounded": true,
      "expect": "FAIL",
      "expect_gates": ["palette_drift_pass"],
      "why": "pose held, colors swapped — must trip drift and only drift"
    },
    {
      "id": "08-NEG-identity-drift",
      "class": "negative",
      "grounded": true,
      "expect": "FAIL",
      "expect_gates": ["silhouette_budget"],
      "why": "four different characters under a shared palette — silhouette must be what catches it"
    },
    {
      "id": "09-NEG-no-gutter",
      "class": "negative",
      "grounded": true,
      "expect": "FAIL",
      "expect_gates": ["recover|slice"],
      "why": "no transparent columns — must fail before the coherence gates, not silently yield one frame. A ground line spanning the canvas trips raw_clipping first (verified synthetically), so either layer is an acceptable catch"
    }
  ]
}
