TRUE chunky pixel art sprite strip ONLY. Four WALK CYCLE frames of a small fantasy MINER facing RIGHT in one horizontal row.

**Canvas:** very wide panoramic strip — aspect ratio roughly 3:1. Each frame **16×24 logical pixels rendered as large crisp square blocks**. No anti-aliasing.

Miner: green tunic, blue helmet, brown pickaxe. **Same palette every frame.** Standard walk poses.

**CRITICAL loop structure:**
- Frame 0: contact pose (right leg forward).
- Frame 1: almost identical to frame 0 (only 1–2 pixels different).
- Frame 2: small step from frame 1 — modest leg change.
- Frame 3: **almost identical to frame 2** (only 1–2 pixels different from frame 2).

Frame 3 must NOT match frame 0 (broken loop), but frame 3 must match frame 2 closely.

Two magenta gutter cells between frames. Flat magenta `#ff00ff` background. One row only.

<!-- Gate control: walk / loop_closure_pass. f3≈f2, sequential neighbors small, f3≉f0. -->
