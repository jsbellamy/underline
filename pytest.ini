[pytest]
# Corpus tests are CPU-bound and independent, so fan them out across cores.
# `worksteal` keeps the long CLI subprocess tests from stranding a worker.
# Pass `-n0` to disable for interactive debugging.
addopts = -n auto --dist worksteal
markers =
    slow: shells out to a budget-derivation CLI that re-derives over the whole corpus
