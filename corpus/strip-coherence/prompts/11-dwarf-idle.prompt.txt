TRUE chunky pixel art sprite strip ONLY. Four idle animation frames of a small fantasy DWARF BLACKSMITH in one horizontal row, facing RIGHT.

**Canvas:** very wide panoramic strip — aspect ratio roughly 3:1 (much wider than tall). The four frames must fill most of the width.

Each frame on an exact **16×24 logical pixel grid rendered large** — every logical pixel is one clean flat square block. No anti-aliasing, blur, gradient, or dithering.

Dwarf: brown beard, leather apron, hammer resting on the shoulder. Feet planted on the **bottom row of each frame**. Idle = a slow hammer tap motion that never lifts the feet off the ground row.

Between each frame: **two full magenta logical cells** of empty gutter (no character pixels in gutter columns).

Flat solid **magenta `#ff00ff`** background everywhere else. Selective 1px dark warm outline. Upper-left light. **8–12 flat colors max**.

Frame order left→right: idle_0, idle_1, idle_2, idle_3 — **same character, same proportions, loopable**.

Do not add margins, labels, numbers, or extra rows. One row of four frames only.
