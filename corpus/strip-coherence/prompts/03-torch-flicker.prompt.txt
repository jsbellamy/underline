TRUE chunky pixel art sprite strip ONLY. Four flicker animation frames of a wall-mounted TORCH in one horizontal row.

**Canvas:** very wide panoramic strip — aspect ratio roughly 3:1 (much wider than tall). The four frames must fill most of the width.

Each frame on an exact **16×24 logical pixel grid rendered large** — every logical pixel is one clean flat square block. No anti-aliasing, blur, gradient, or dithering.

Torch: a brown wooden sconce in the **lower third, identical in every frame — do not move or recolor the sconce**. Above it, a flame of 5–8 blocks that flickers: the flame's outline shifts by one or two blocks per frame, and the hot core block moves between yellow and orange.

Between each frame: **two full magenta logical cells** of empty gutter (no character pixels in gutter columns).

Flat solid **magenta `#ff00ff`** background everywhere else. Selective 1px dark outline. **8–12 flat colors max** — the flame uses at most three warm colors (deep orange, orange, yellow).

Frame order left→right: flicker_0, flicker_1, flicker_2, flicker_3 — **loopable**.

Do not add margins, labels, numbers, or extra rows. One row of four frames only.

<!-- corpus note: legitimate emissive color churn in the flame with a fixed sconce.
     This is the honest stress case for palette_drift — a real animation whose colors
     genuinely shift frame to frame. If drift > 0.15 here, the drift budget is keyed
     to non-emissive sprites and needs an emissive class. Expect PASS but watch drift. -->
